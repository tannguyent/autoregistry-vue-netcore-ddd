import { ActionTree } from "vuex";
import { IRootState } from "../../../../../store/root-state";
import { IPermissionState } from './state';

/**
 * VUEX ACTION
 * */
export const actions: ActionTree<IPermissionState, IRootState> = {
    
};
