import Vue from 'vue';
import { Component } from 'vue-property-decorator';

@Component({
    template: require('./index.html'),
    components: {
    }
})
export default class GridContainerComponent extends Vue {
}