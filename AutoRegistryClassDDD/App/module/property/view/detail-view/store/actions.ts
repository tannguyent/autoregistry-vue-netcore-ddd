import { ActionTree } from "vuex";
import { IRootState } from "../../../../../store/root-state";
import { PropertyViewState } from "./state";

/**
 * VUEX ACTION
 * */
export const actions: ActionTree<PropertyViewState, IRootState> = {
};