import { GetterTree } from 'vuex';
import { IRootState } from "../../../../../store/root-state";
import { IPropertyEditState } from './state';

/**
 * VUEX: GETTER
 * */
export const getters: GetterTree<IPropertyEditState, IRootState> = {
};